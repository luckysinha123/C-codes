#include <stdio.h>

int main() {
int num,r,arm=0;
scanf("%d",&num);
int original = num;
while (num>0){
    r=num%10;
    arm = (r*r*r)+arm;
    num=num/10;
}
if(original==arm){
    printf("armstrong");

}
else{
    printf("Not armstrong");
}
return 0;
}
